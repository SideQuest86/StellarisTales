# Stellaris Tales · AI 开发入口

## 产品约束
- **第二版覆盖旧界面约束**：大类对应三维长列，列内滑动完整故事档案；持续呼吸、切换波浪和抬起展开；阅读界面不显示任何效果脚本、技术条件、事件 ID 或来源代码。原始溯源只留在开发数据层。加入原版 OST 专辑循环与切歌。
- 本地《群星》安装目录是正文、选项、中英本地化、原版美术的第一来源。禁止生成式美术替代原版资产。
- 全量收录本地安装基础游戏及 DLC 中的事件；额外收录考古、星界裂隙、局势、特殊项目与事件链定义。安装文件可用不等同于用户已启用/拥有该 DLC；不假装读取了存档或工坊订阅。
- 条件、随机、作用域及延迟必须保留。静态文档不是游戏模拟器，不对运行时条件求值。
- 参考 RhineLabUI 的档案阵列、抽取、波浪、文字揭示、快速反向连续性；原参考目录只读，不移植其品牌或其他游戏美术。
- 用户已授权实现、简单可视化测试、GitHub 发布及在线浏览链接。

## 结构
- `scripts/extract.py`：读取游戏和 DLC ZIP，解析 Paradox 脚本和双语本地化；转换原版 DDS，输出可追溯内容。
- `public/data/`：生成的索引、按来源分片的故事、覆盖报告。不要手改生成文件。
- `public/art/`：从游戏转换的网页图片；版权不归本项目代码所有。
- `src/`：浏览器端阅读器、动效、样式，与提取器独立。
- `content/`：人工分类与有来源的补充注释（与原文区分）。
- `scripts/build.mjs` / `serve.mjs`：含 Three.js 本地打包的静态构建与本地服务器，支持 GitHub Pages 子路径。
- `tests/`：提取准确性、分支和构建完整性测试。
- `verification/`：浏览器验证与已知限制。
- `.github/workflows/`：GitHub Pages 发布。

## Milestones
- [x] L1 起源故事：逐源起源归属、阶段与间接调用、分支可达性、连续遍历、浏览器阅读。
- [x] L2 先驱者与遗迹：事件链、特殊项目、考古阶段、条件视角、连续遍历。
- [x] L3 深空异闻：异常与调查项目、剧情结局及后续、连续遍历。
- [x] L4 星界裂隙：阶段事件与选择、回环、起源关联、连续遍历。
- [x] L5 银河天灾：危机阶段、共享通讯、后续事件与终局、连续遍历。
- [x] L6 星神与巨兽：遭遇、战斗后续、城邦交互及跨篇连接、连续遍历。
- [x] L7 文明纪事：殖民地、局势、外交与社会事件、连续遍历。
- [ ] L8 银河轶事：其余故事归属与孤立篇章、全量逻辑和视觉回归、线上发布。
- [x] V2.1：按完整剧情编组，保留全部原记录、篇章及选择，区分共享事件与故事关系。
- [x] V2.2：参考截图的近景长列阵列、惯性、列内记忆、呼吸/波浪、抽取与归位。
- [x] V2.3：沉浸式章节阅读，无代码、机械条件或 ID；完整故事目录与选择回溯。
- [x] V2.4：原版 23 首 OST 专辑、播放/暂停、切歌、曲目列表、进度与专辑循环。
- [ ] V2.5：桌面/手机可视化、全量内容约束与音乐实际播放验证，发布至原 GitHub Pages 地址。
- [x] M0：确认空仓库、参考目录及本地安装版本 Pegasus v4.4.6 (fdde)。
- [x] M1：全量提取、来源清单、解析验证及双语/图片覆盖统计。
- [x] M2：档案浏览、分类检索、双语阅读及可回溯分支。
- [x] M3：参考动效、响应式、减少动态效果与性能预算。
- [x] M4：自动化及桌面/移动可视化验证。
- [x] M5：GitHub 提交/推送、部署及线上检查。

## 当前发现
- 基础 `events/` 有 170 个文件；必须扫描 DLC ZIP，不能只计基础目录。
- 游戏文件可直接只读访问。文本为 Paradox 脚本与带版本标记的本地化 YAML。
- 灰机 Wiki 首页 2026-09-10 返回 403；未将未获取的 Wiki 内容当作已验证补充。
- 提取记录原始路径与 SHA-256；使用文件级确定性排序，重复定义和无法解析内容进入报告。

## 交接规则
开始前阅读本文件与覆盖报告；完成每个 milestone 时更新证据和限制。新解析语法添加有意义的回归样本。内容注释需注明来源、版本和是否为解读。任何分工以互不重叠文件范围为边界，合并后统一检查输出格式。

## 2026-09-10 / Draft 1 实现证据
- 11,347 条独立记录：10,082 个事件节点与 1,265 个剧情定义；8,476 条含可读文本且非隐藏记录。256 个来源分片，1,459 份输入来源清单。
- 570 个有效原版图片映射；0 图片解析错误、0 脚本解析错误、0 内联模板错误、0 重复 ID、0 无法定位的静态链接目标。中英各 3 个原始缺失键见覆盖报告。
- `event = {}` 也是游戏事件，不能只识别 `*_event`。关键字大小写不敏感，值和本地化键保留大小写。内联模板递归参数替换后解析；展开文件行号以 `lineSpace=expanded` 标注。
- 特殊项目 ID 用 `key`/`KEY`；简介可能使用 `_DESC`。星界裂隙与考古 `event = id` 为阶段连接；启用特殊项目与开始事件链也建立可导航边。
- `localization.sources` 区分缺失条目与游戏故意定义的空字符串，空字符串不可回退显示键名。
- 原版插图只读取记录顶层 `picture`，不能把效果中的 `set_planet_entity.picture` 误当事件图。
- 完整原脚本（或内联展开脚本）始终可读；尚不展开所有 scripted effect/on_action 的间接图，不模拟随机或存档状态。
- 性能预算：7 张活动档案卡、每页最多 24 张结果卡；按来源懒加载，最多 8 份分片缓存；阻尼动画结束/页面隐藏时停止渲染；遵循系统与手动减少动态效果设置。
- 校验证据见 `verification/README.md`、`browser-results.json`、`performance.json`。手机尺寸验证不等同于真实移动硬件性能认证。
- GitHub 仓库 `SideQuest86/StellarisTales` 已由用户主动改为 public；代理不擅自改变仓库可见性。
- 用户额外授权：仅账户剩余额度低于 3% 时可自动使用一次重置，本任务最多一次。已检查剩余 36%，未使用重置。
- GitHub 已成功推送初稿 `2add421`。Pages API 返回 422：当前套餐不支持此私有仓库的 Pages。采用 Sites 托管；不修改 GitHub 可见性。工作流默认只测试/构建，将来开通 Pages 后设置 Actions 变量 `ENABLE_GITHUB_PAGES=true`。
- 上条为发布过程记录：用户随后主动将仓库改为 public。现已成功启用 GitHub Pages（workflow 模式），并设置 `ENABLE_GITHUB_PAGES=true`。实际发布目标恢复为 `https://sidequest86.github.io/StellarisTales/`。
- Sites 曾作为备用方案完成注册；`.openai/hosting.json` 保留该唯一 ID，尚未推送源或部署。不要重复创建 Sites，也不要把未部署的备用站当作在线地址。

## Draft 1 发布完成
- 在线地址：<https://sidequest86.github.io/StellarisTales/>
- 部署源提交：`02575eb8cee03ca507bbd4c12f25543e16be3344`。
- GitHub Actions 成功运行：<https://github.com/SideQuest86/StellarisTales/actions/runs/34518086926>。
- 线上检查时间：2026-09-10T19:04:40Z；HTTP 200、子路径资源加载正常、跨分片的视界信号 → 特殊项目 → 引力即欲望可正常访问，两个原版选项保留，浏览器异常为 0。
- 证据：`verification/online-results.json`。此后的文档归档提交不改变已部署的应用代码。
- 后续优先方向：按具体事件链精修分类与导读；补充可追溯 Wiki 解读；解析 scripted_effect/on_action 间接连接；真实移动设备验证。原始内容已经全量收录，不以人工精选取代底层索引。

## Draft 2 实现与维护入口
- `scripts/compile-stories.py` 从原始数据编译 `public/stories/`，`content/story-families.json` 保存优先于自动关联的故事族规则。全部 11,347 条记录各归属一个档案；2,258 个档案中 2,160 个有可读剧情，合计 8,534 个可读篇章。后台附录不展示为故事卡。
- 关联依据为事件引用、项目/阶段连接、同命名空间稀有剧情标记，以及明确的故事族。通用系统事件与高入度共享节点不强行合并；5,276 条跨档案关系由阅读器原地加载后续篇章，保持当前档案标题。自动分组尚未逐条人工校订；完善故事族规则后重编译，不手改输出。
- `src/archive-scene.js` 使用 Three.js 的实例化网格：7 列 × 41 行可见池；类别与列内故事循环；相机投影逆变换拖动、真实释放速度、摩擦与临界阻尼、列内位置记忆。`archive-motion.js` 维护独立运动数学。
- 参考阅读范围：RhineLabUI 的 archive-drag、archive-loop、motion、render-quality、scene；吸收有限实例池、双轨拖动、选中波浪和纯竖直抽取逻辑。本站重新实现，未复制其品牌、美术或源文件。
- 正常状态持续呼吸，空闲绘制约 30 fps；运动时随屏幕刷新。287 实例、约 8 次主绘制调用（另有阴影绘制）；像素比上限 1.5，图片缓存 24 项。减少动态效果时稳定后停止循环，隐藏页暂停。旧版“空闲完全停绘”约束已被新需求覆盖。
- `src/app.js` 仅消费叙事数据；章节、原版选择、中英文、回溯与目录在同一阅读器中。原脚本留在 `public/data/`，阅读器没有代码入口。动态名称以叙事占位措辞呈现，条件文本保留为不同讲述，不模拟存档条件或随机结果。
- `scripts/import-ost.py` 从本地 soundtrack/mp3 导入 23 首原始 MP3（约 162 分钟、385 MB），清单保存 ID3 与 SHA-256。`src/audio-player.js` 负责点击开始播放、上一首/下一首、列表、进度、音量与自然结束后的专辑循环；不预加载整张专辑。
- 安装依赖：pnpm install --frozen-lockfile。Python 内容更新需要 Pillow、mutagen。构建仅清理经路径检查的生成目录 dist，再复制当前数据和 Three.js 模块/许可证。
- 当前验证入口：node --test tests/*.test.mjs；python -m unittest discover -s tests -p "test_*.py"；node scripts/verify-v2.mjs。旧 V1 视觉脚本已移除，历史结果仅作归档。
- 浏览器验证含真实 MP3 播放、曲尾自然循环、完整故事跨项目跳转、双语、目录、搜索、深链接、手机布局和减少动态效果。报告中的 requestAnimationFrame 间隔只是当前桌面浏览器调度采样，不是手机 GPU 性能保证。

## Draft 2 最新用户修订与实现
- 参考截图及源码只指导 layout 和动画，不能照搬白色 RhineLab 美术。当前使用长焦近景、斜向长列、画面外延伸、右侧文字锚点、选中文件纯竖直抽取；群星深色金属、金色细节、原版图和 OST。
- `origin_families.py` 与 `content/origin-families.json` 读取正式起源定义，58 个起源总览作为额外合成章节，原始 11,347 条记录不增减。起源简介去除帝国创建界面的 WARNING 限制提示。演化猎手和野性呼唤分别为 39、42 篇；繁荣一统只有原版简介，不编造不存在的专属剧情。共享起源事件作为目录引用原地读取。
- 起源数据、623 个图像映射和 1,508 份输入来源都来自本地安装。原始层的 8,476 个可读节点加 58 个起源总览，阅读层共 8,534 篇。
- `scripts/reactions.py` 编译文明视角，标签来自游戏本地化。处理布尔条件、不同人口作用域、共同文本加 if/else 后缀、switch 选文；绑定对应选项。相同原文且相同跳转的机械效果差异合并展示。少量视角用标签，多视角用下拉选择，避免堆满页面。条件组合有界，不宣称复现全部游戏脚本语义。
- 正文桌面 19px、手机 18px，衬线字体、较宽行距和短淡入。删除装饰性标语；保留必要标题、导航与原文。
- 最新本地验证见 verification/v2-results.json；17 项 Node 测试和 7 项 Python 测试通过。浏览器实测涵盖两种 Cybrex 原文、噬杀蜂群正文/选项配对、起源目录、原声自然循环、投影拖动和手机布局。
- 最近额度读取：已用 73%，剩余 27%，未使用重置。仅低于 3% 可按用户授权使用一次。


## 最新布局与逐类阅读验收（2026-09-10）
- 阵列采用 287 个循环实例，左右端均在屏幕外；统一封面在抽取过程中擦显为当前章节原版图片。快速反向选择接续原位移和速度。桌面左侧为模型与事件图，右侧为档案文本；手机保留正文上方事件图。类别导航在顶部。
- `scripts/story_navigation.py` 输出每份档案的完整阅读顺序、分叉树和原始内部连接；`src/story-map.js` 负责树图与未读导航。每章可通过“下一篇”访问剩余全部章节，原版选择单独保留。实线代表已解析的剧情连接；虚线只补足阅读顺序，不声称为游戏因果或所有分支同时发生。
- 提取器补入 7,879 条 scripted effect 静态调用连接。通用 helper 不用于合并独立故事。字符串参数递归解析有深度上限，不等同完整游戏引擎；列表参数、存档状态和动态目标不保证全部求值。
- 每类全量检查原始边保留、章节唯一性、共享引用与从任意起点连续遍历；另用真实浏览器点击一个代表故事的全部篇章并检查目录树。证据在 `verification/categories/`。

| Milestone | 可读档案 | 篇章（含共享引用） | 浏览器完整遍历代表 |
|---|---:|---:|---|
| L1 起源 | 58 | 1529 | 野性呼唤，42 篇 |
| L2 先驱者 | 138 | 703 | 赛博勒克斯，42 篇 |
| L3 深空异闻 | 309 | 917 | 视界信号，75 篇 |
| L4 星界裂隙 | 62 | 434 | 27 篇裂隙档案 |
| L5 天灾 | 75 | 642 | 肃正协议，60 篇 |
| L6 星神与巨兽 | 51 | 304 | 60 篇巨兽档案 |
| L7 文明纪事 | 363 | 1180 | 科技进步，68 篇 |
| L8 银河轶事 | 1104 | 2829 | 虚境的低语，114 篇；本地验收通过，发布待完成 |

自动验收证明数据可达性和交互连贯性，不代替逐条游戏存档验证。后续修订应调整编译规则和回归样本，不能把人为阅读桥伪装成原版选择。

### 左侧特写与磨砂换章
- 开档时先抬升，再用连续四元数插值转向镜头并放大到左半屏完整特写。模型与原阵列保持同一场景，关闭反向归位。
- 原版图片使用同一 GPU 纹理更新；切章先将旧图磨砂化，遮盖后更新图片，再以菱形/斜向分面软边揭示新图。使用固定 9 次纹理采样与静态细颗粒，避免动态噪声闪烁及整屏后处理。
- 快速连续切章以最后一次请求为准，图片解码完成才揭示；减少动态效果立即完成过渡。桌面右侧只展示正文、视角及分支。
